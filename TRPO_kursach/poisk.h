#pragma once

#include "main.h"
#include "check.h"
#include "vecStudWork.h"

bool poiskAverageBall(vector<Student>); //поиск записей о студентах по среднему баллу
bool poiskFio(vector<Student>);         //поиск записей о студентах по ФИО
bool poiskGroup(vector<Student>);       //поиск записей о студентах по группе
void poiskStud(vector<Student>);        //меню поиска записей о студентах
